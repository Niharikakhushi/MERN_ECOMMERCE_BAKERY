import react from 'react';

const Props = (props) => {
  return (
    <div>
      {/* <h1>{props.name}</h1> */}
      {/* <h1>{props.name}</h1> */}
      <h2>Name is {props.secondprops.name} and age is {props.secondprops.age} </h2>
      
    </div>
  );
};

export default Props;